# Copy this file to `.env` at the repo root and fill in your keys.
# Never commit `.env`.

# --- OpenAI (gpt-image-2 carousels and single-image batch) ---
OPENAI_API_KEY=sk-proj-...

# --- Google Gemini (optional — only if you want to run the original Gemini scripts) ---
GEMINI_API_KEY=
